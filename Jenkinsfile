pipeline {
    agent { label 'master' }
    stages {
        stage('git'){
            steps{
                git credentialsId: 'github', url: 'https://github.com/arun1252/IBMcloud.git' , branch:'master'
            }
        }
		}
		}
